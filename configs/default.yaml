seed: 42
device: auto

train:
  img_size: 256
  batch_size: 8
  num_workers: 2
  epochs: 15
  labeled_frac: 0.1

optim:
  lr: 1.0e-4
  weight_decay: 0.0

lr_schedule:
  step_epochs: 10
  gamma: 0.5

loss:
  dice_weight: 20.0
  focal_weight: 1.0
  focal_alpha: 0.25
  focal_gamma: 2.0
